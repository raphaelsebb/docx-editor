---
'@eigenpal/docx-editor-core': patch
---

Reserve the document scrollbar gutter on both edges in the React editor so the page stays centered on platforms with classic scrollbars, matching the Vue editor. Fixes #888
