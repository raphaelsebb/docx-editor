---
'@eigenpal/docx-editor-vue': patch
---

Fix the document outline toggle button and outline panel overlapping the ruler in the Vue editor when the ruler is shown. Both now sit below the ruler row, matching React. Fixes #887
